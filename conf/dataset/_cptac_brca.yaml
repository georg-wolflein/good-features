name: cptac_brca
clini_tables: ["${settings.metadata_dir}/CPTAC-BRCA_CLINI.csv"]
slide_tables: ["${settings.metadata_dir}/CPTAC-BRCA_SLIDE.csv"]
feature_dirs: ["${settings.features_dir}/CPTAC-BRCA/${settings.feature_extractor}"]
folds_table: "${settings.folds_dir}/${.name}.csv"
cache_dir: "/data/histaug/cache/CPTAC-BRCA/${settings.feature_extractor}/${.augmentations.name}"
filename_col: FILENAME
patient_col: PATIENT
